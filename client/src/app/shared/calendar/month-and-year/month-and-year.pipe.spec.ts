import { MonthAndYearPipe } from './month-and-year.pipe';

describe('MonthAndYearPipe', () => {
  it('create an instance', () => {
    const pipe = new MonthAndYearPipe();
    expect(pipe).toBeTruthy();
  });
});
